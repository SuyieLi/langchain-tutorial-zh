# 00 · 学习路径与生态全景

> LangChain 是什么、生态里有哪些组件、1.0 版本怎么装、本项目的模型配置约定、学习路线怎么走。
> 下一篇：[[01-环境搭建与第一个程序]]

---

## 1. LangChain 是什么

**LangChain 是一个用于构建 LLM（大语言模型）应用的框架**，核心理念：

> 不直接和某个厂商的模型 API 死绑，而是通过**统一抽象层**，让"换模型像换电池一样简单"。

它解决的核心问题：

| 痛点 | LangChain 的答案 |
|------|------------------|
| 每家模型 API 格式不同 | 统一 `BaseChatModel` 接口，一行切换厂商 |
| 应用逻辑越来越复杂 | LCEL / Runnable 组合式编程 |
| 需要调用外部工具 | 标准 Tool 抽象 + 工具调用循环 |
| 模型知识过期、会胡说 | RAG 检索增强，把资料喂给模型 |
| 复杂多步任务 | Agent 自主决策循环 + LangGraph 编排 |

> [!note] 一句话理解
> 如果说 GPT 等模型是"发动机"，LangChain 就是"整车框架"——它负责组装：方向盘（Prompt）、油门（模型）、仪表盘（输出解析）、导航（Agent）。

---

## 2. LangChain 生态全景（1.0）

LangChain 不只是"一个包"，而是**一个生态**，由以下核心部分组成：

```
┌─────────────────────────────────────────────────┐
│                  你的应用 / Agent               │
├─────────────────────────────────────────────────┤
│  LangChain (主包)     高层抽象：create_agent     │
│  ┌───────────────────────────────────────────┐ │
│  │ langchain-core      核心接口 (Runnable等) │ │
│  └───────────────────────────────────────────┘ │
│  langchain-openai / anthropic / ... 厂商集成包  │
│  langchain-community   社区集成 (加载器/向量库) │
│  langchain-classic     0.x 旧 API 兼容包        │
├─────────────────────────────────────────────────┤
│  LangGraph  低层图编排（状态机、持久化、HITL）  │
│  LangSmith  可观测性 + 评估（链路追踪/数据集）  │
└─────────────────────────────────────────────────┘
```

### 2.1 各包职责（1.0 包结构）

| 包 | 职责 | 安装 |
|----|------|------|
| `langchain-core` | **核心抽象**：`Runnable`、`BaseChatModel`、消息类型、LCEL。无第三方依赖 | 随主包自动装 |
| `langchain` | **主包**：`create_agent`、`init_chat_model`、`@tool` 等高层 API，自动重导出 core | `pip install langchain` |
| `langchain-openai` 等 | 官方厂商集成包（OpenAI/Anthropic/Google…） | 按需装 |
| `langchain-community` | 社区集成：文档加载器、向量库、杂项 | `pip install langchain-community` |
| `langchain-classic` | 0.x 旧 API（`LLMChain`、旧 `AgentExecutor` 等）兼容层 | 旧项目迁移用 |
| `langgraph` | 图编排引擎，`create_agent` 底层就是它 | `pip install langgraph` |
| `langsmith` | 链路追踪与评估（生产必备） | `pip install langsmith` |

### 2.2 1.0 的推荐导入路径

```python
from langchain.agents import create_agent          # 创建智能体
from langchain.chat_models import init_chat_model  # 统一模型初始化
from langchain.tools import tool                   # 定义工具
from langchain.messages import HumanMessage, AIMessage  # 消息类型
```

> [!warning] embedding 的特殊情况
> 网上常见 `from langchain.embeddings import init_embeddings`，但**本项目默认用 DeepSeek，而 DeepSeek 不提供 embedding 接口**。因此 RAG 篇（[[08-RAG检索增强生成]]）改用**本地 HuggingFace embedding**（`langchain_huggingface.HuggingFaceEmbeddings`，免 Key、离线）。见下方「本项目模型配置约定」。

> ⚠️ 网上大量 0.x 教程的 `from langchain.chat_models import ChatOpenAI`、`from langchain.llms import OpenAI` 在 1.0 中已废弃，请统一使用上面的新路径。

---

## 3. 本项目模型配置约定（重要，全 code/ 统一）

本教程所有示例**默认走 DeepSeek**（国内模型，便宜好用，兼容 OpenAI 协议，无需科学上网）。配置全部从项目根的 `.env` 读取，不在代码里硬编码密钥。

### 3.1 `.env` 四件套

复制 `.env.example` 为 `.env`，填入：

```ini
API_KEY=sk-你的deepseek密钥
MODEL_NAME=deepseek-chat
MODEL_PROVIDER=openai            # DeepSeek 兼容 OpenAI 协议，provider 仍填 openai
BASE_URL=https://api.deepseek.com/v1
```

### 3.2 每个示例顶部的固定写法

```python
from dotenv import load_dotenv
load_dotenv()
import os
api_key = os.getenv("API_KEY", "")
model_name = os.getenv("MODEL_NAME", "deepseek-chat")
model_provider = os.getenv("MODEL_PROVIDER", "openai")
base_url = os.getenv("BASE_URL", "https://api.deepseek.com/v1")

model = init_chat_model(
    model_name,
    model_provider=model_provider,
    api_key=api_key,
    base_url=base_url,
)
```

### 3.3 想换模型？只改 `.env`，代码不动

| 模型 | MODEL_PROVIDER | MODEL_NAME | BASE_URL | API_KEY |
|------|----------------|------------|----------|--------|
| DeepSeek（默认） | `openai` | `deepseek-chat` | `https://api.deepseek.com/v1` | 你的 key |
| 通义千问 | `openai` | `qwen-plus` | `https://dashscope.aliyuncs.com/compatible-mode/v1` | 你的 key |
| OpenAI | `openai` | `gpt-4o-mini` | （留空用默认） | 你的 key |
| Ollama（本地，免 Key） | `ollama` | `qwen2.5:7b` | `http://localhost:11434` | 留空 |

> [!tip] Ollama 是唯一完全免 Key 的选项
> 它把模型跑在你自己电脑上，离线免费。DeepSeek/通义/OpenAI 都是云服务，必然要 Key。详见 [[01-环境搭建与第一个程序]]。

### 3.4 DeepSeek 的三个已知限制（避坑，详见 [[12-常见问题与避坑指南]]）

1. **预付费，余额不足会返回 402**：key 有效 ≠ 能调用，账号必须有余额。
2. **不提供 embedding 接口**：RAG 必须改用本地 HuggingFace embedding。
3. **`trim_messages` 不能用 `token_counter=model`**：会抛 `NotImplementedError`，需自定义计数器。

---

## 4. 框架哲学：三个关键抽象

LangChain 的一切都围绕三个抽象展开，理解它们就理解了框架：

### 4.1 Runnable（可运行对象）——一切皆可链

所有组件（Prompt、模型、解析器、检索器）都实现了 `Runnable` 接口，统一提供：

- `invoke()` — 单次调用
- `stream()` — 流式输出
- `batch()` — 批量处理
- `ainvoke()/astream()/abatch()` — 异步版本

这正是 **LCEL**（[[05-LCEL表达式语言]]）的基础：用 `|` 把 Runnable 串成管道。

### 4.2 Message（消息）——与模型的统一语言

```
HumanMessage (用户) → SystemMessage (系统) → AIMessage (模型) → ToolMessage (工具结果)
```

### 4.3 Tool（工具）——模型的"手和脚"

模型本身不能查数据库、不能发请求，通过 `@tool` 定义工具，让模型自主决定何时调用（[[07-工具与函数调用]]）。

---

## 5. LangChain vs LangGraph vs LangSmith（重要！）

这是新手最容易混淆的三兄弟：

| | LangChain | LangGraph | LangSmith |
|---|---|---|---|
| 定位 | 高层抽象，快速构建 | 低层编排，精细控制 | 观测 + 评估平台 |
| 类比 | 自动挡汽车 | 手动挡 / 赛车 | 行车记录仪 + 4S 店 |
| 核心 API | `create_agent` | `StateGraph` | `tracing` / `evaluate` |
| 适用场景 | 90% 的常规应用 | 复杂流程、状态管理、人机协同 | 生产环境调试优化 |
| 关系 | **构建在 LangGraph 之上** | LangChain 的引擎 | 二者共用的观测层 |

> [!tip] 选型口诀
> - 简单问答 / 单轮工具调用 → **LangChain** 够用
> - 多步流程、需要状态持久化、人类审批 → **LangGraph**
> - 上线前先接 **LangSmith** 做追踪和评估

> [!warning] LangSmith 是独立平台，需单独注册
> DeepSeek 是「被测模型」，LangSmith 是「评估平台」，二者不能互替。本项目在无 LangSmith Key 时提供了**本地评估降级模式**（见 [[11-生产实践与LangSmith]]）。

---

## 6. 学习路线图（本教程怎么学）

| 篇号 | 主题 | 对应代码 | 核心能力 |
|------|------|----------|----------|
| 00 | 学习路径与生态全景 | — | 建立全局认知 |
| 01 | 环境搭建与第一个程序 | `01_hello_langchain.py` | `.env` 配置 + `init_chat_model` |
| 02 | 模型调用基础 | `02_models.py` | 消息 / invoke·stream·batch / 结构化输出 |
| 03 | Prompt 工程与模板 | `03_prompt_templates.py` | ChatPromptTemplate / Few-shot |
| 04 | 输出解析与结构化输出 | `04_structured_output.py` | `with_structured_output` / Pydantic |
| 05 | LCEL 表达式语言 | `05_lcel.py` | 管道 / 并行 / 分支 / 透传 |
| 06 | 记忆与多轮对话 | `06_memory.py` | RunnableWithMessageHistory / trim_messages |
| 07 | 工具与函数调用 | `07_tools.py` | `@tool` / bind_tools / create_agent |
| 08 | RAG 检索增强生成 | `08_rag.py` | 本地 embedding / Chroma / 检索链 |
| 09 | Agent 智能体 | `09_agent.py` | create_agent / ToolStrategy / 流式 |
| 10 | LangGraph 工作流编排 | `10_langgraph_workflow.py` | StateGraph / 条件边 / 检查点 / HITL |
| 11 | 生产实践与 LangSmith | `11_langsmith_eval.py` | 评估 / 本地降级 / 链路追踪 |
| 12 | 常见问题与避坑指南 | — | DeepSeek 坑 / 0.x 迁移 / FAQ |

### 阶段一：地基（第 00–04 篇）
打通"装环境 → 调模型 → 写 Prompt → 拿结构化结果"的完整闭环，先建立手感。

### 阶段二：进阶（第 05–08 篇）
掌握组合式编程（LCEL）、对话记忆、工具调用、RAG——这四个能力构成绝大多数生产应用。

### 阶段三：高级（第 09–12 篇）
Agent 决策循环、LangGraph 编排、可观测性与评估，以及实战中的坑。

### 建议节奏
- **每天 1–2 篇**，配合 `code/` 目录运行代码，**务必亲手跑一遍**；
- 每篇末尾有"动手练习"，做完再进入下一篇；
- 遇到概念用 [[12-常见问题与避坑指南]] 查漏补缺。

---

## 7. 官方资源

| 资源 | 地址 |
|------|------|
| 官方文档（1.0 新站，中英双语教程） | https://docs.langchain.com |
| 中文社区文档 | https://langchaincn.cn/docs.html |
| GitHub（langchain-ai/langchain） | https://github.com/langchain-ai/langchain |
| 官方博客（1.0 发布说明） | https://blog.langchain.com/langchain-langgraph-1dot0 |
| API 参考 | https://docs.langchain.com/api-reference |

---

## ✅ 动手练习

1. 用一句话向朋友解释：LangChain 和 ChatGPT 的关系是什么？
2. 说出 1.0 时代推荐的 4 个核心导入路径（`from langchain.xxx import yyy`）。
3. 把 `.env.example` 复制为 `.env`，填入你的 DeepSeek（或其它模型）配置——后续所有示例都靠它。
4. 思考：你的学习场景里，哪些地方会用到 Agent，哪些地方用普通链就够了？

---

🏷️ `#LangChain` `#学习笔记` `#生态全景` `#LangGraph` `#LangSmith` `#DeepSeek`

[[README|← 返回学习路径总览]] ｜ [[01-环境搭建与第一个程序|下一篇：环境搭建 →]]
