# 02 · 模型调用基础

> 深入理解 Chat Models、`init_chat_model` 统一入口、消息结构与四种调用方式。
> 上一篇：[[01-环境搭建与第一个程序]] ｜ 下一篇：[[03-Prompt工程与模板]]

---

## 1. 模型的两大类型：LLM vs Chat Model

LangChain 中模型分两类，1.0 后**统一推荐 Chat Model**：

| 类型 | 说明 | 现状 |
|------|------|------|
| **Chat Model**（聊天模型） | 基于消息列表（messages）交互，支持多轮、工具调用、结构化输出 | ✅ 1.0 标准 |
| LLM（补全模型） | 基于纯字符串补全，能力弱、无消息概念 | ⚠️ 已废弃，仅 `langchain-classic` 保留 |

> [!tip] 记忆口诀
> 现在所有主流模型（GPT、Claude、DeepSeek、GLM…）都是 Chat Model 形态。
> 看到老教程里 `from langchain.llms import OpenAI`，直接按 Chat Model 重写。

---

## 2. `init_chat_model`：跨厂商统一入口（1.0 新 API）

### 2.1 本项目的标准写法（默认 DeepSeek）

本项目所有示例从 `.env` 读配置（见 [[01-环境搭建与第一个程序]] 第 3 节），统一写法：

```python
from dotenv import load_dotenv
from langchain.chat_models import init_chat_model
import os

load_dotenv()
model = init_chat_model(
    os.getenv("MODEL_NAME", "deepseek-chat"),
    model_provider=os.getenv("MODEL_PROVIDER", "openai"),
    api_key=os.getenv("API_KEY", ""),
    base_url=os.getenv("BASE_URL", "https://api.deepseek.com/v1"),
)
```

> DeepSeek 兼容 OpenAI 协议，所以 `model_provider` 填 `openai`，只需把 `base_url` 指向 DeepSeek。

### 2.2 切换厂商 = 改 `.env`，代码不动

| 厂商 | MODEL_PROVIDER | MODEL_NAME | BASE_URL |
|------|----------------|------------|----------|
| DeepSeek（默认） | `openai` | `deepseek-chat` | `https://api.deepseek.com/v1` |
| OpenAI | `openai` | `gpt-4o-mini` | （留空） |
| Anthropic | `anthropic` | `claude-sonnet-4-5` | — |
| 通义千问 | `openai` | `qwen-plus` | `https://dashscope.aliyuncs.com/compatible-mode/v1` |
| 本地 Ollama | `ollama` | `qwen2.5:7b` | `http://localhost:11434` |

**这就是 LangChain 最值钱的能力：供应商解耦。**

### 2.3 常用参数

```python
model = init_chat_model(
    "deepseek-chat",
    model_provider="openai",
    api_key=api_key,
    base_url=base_url,
    temperature=0.7,        # 随机性：0=稳定可复现，1=天马行空
    max_tokens=2048,        # 最大生成长度
    timeout=30,             # 超时（秒）
    max_retries=2,          # 失败重试次数
)
```

| 参数 | 作用 | 建议 |
|------|------|------|
| `temperature` | 采样温度 | 代码/事实类 0–0.3；创意类 0.7–1.0 |
| `max_tokens` | 输出上限 | 长文任务注意调大 |
| `top_p` | 核采样（与 temperature 二选一） | 一般用 temperature 即可 |
| `timeout` | 请求超时 | 生产建议设短 + 重试 |

---

## 3. 消息结构：与模型的统一语言

Chat Model 的输入输出都是**消息列表**。LangChain 定义了标准消息类型：

```python
from langchain.messages import (
    SystemMessage,      # 系统指令（设定角色/规则）
    HumanMessage,       # 用户消息
    AIMessage,          # 模型回复
    ToolMessage,        # 工具执行结果（见笔记 07）
)

messages = [
    SystemMessage("你是一个资深的Python导师，回答要简洁、带代码示例。"),
    HumanMessage("什么是装饰器？"),
]
resp = model.invoke(messages)
print(resp.content)      # 模型回复文本
print(type(resp))        # <class 'langchain_core.messages.ai.AIMessage'>
```

> 对应代码：`code/02_models.py` 的 `part1_messages()`。

### 3.1 为什么用 Message 而不是字符串？

因为 Chat Model API 本身就是消息序列——上下文就是"消息列表的累积"。用统一 Message 类型，框架才能：

- 记录并重放多轮对话（[[06-记忆与多轮对话]]）
- 处理工具调用消息链（[[07-工具与函数调用]]）
- 跨厂商无痛迁移（结构统一）

### 3.2 1.0 新增：`content_blocks`（内容块）

1.0 引入 `AIMessage.content_blocks` 属性，统一了推理痕迹（reasoning）、引用（citations）、工具调用等复杂内容：

```python
resp = model.invoke("3.14159 保留两位小数是多少？")
for block in resp.content_blocks:
    print(block.type)        # text / reasoning / tool_call ...
    print(getattr(block, "text", None))
```

> 内容块是 1.0 为了支持**推理模型（如 o1、deepseek-r1）**和**服务端工具调用**而设计的标准化结构，属进阶内容，了解存在即可。

---

## 4. 四种调用方式

Runnable 统一提供四类调用（所有模型、链、检索器都支持）：

```python
# 1. invoke：单次调用（最常用）
resp = model.invoke("讲个冷笑话")

# 2. stream：流式输出（打字机效果，聊天应用必备）
for chunk in model.stream("写一首 4 行诗"):
    print(chunk.content, end="", flush=True)

# 3. batch：批量处理（注意调用的是列表）
resps = model.batch(["1+1等于几", "2+2等于几"])
for r in resps:
    print(r.content)

# 4. 异步版（性能优化用）
import asyncio

async def main():
    resp = await model.ainvoke("异步调用示例")
    print(resp.content)

asyncio.run(main())
```

> 对应代码：`code/02_models.py` 的 `part2_invoke_stream_batch()`。

> [!tip] 流式输出原理
> `stream()` 每次产出一个**增量块（chunk）**，前端每收到一块就渲染，形成"逐字输出"的效果。
> 大模型应用几乎都该用流式，用户体验差异巨大。

---

## 5. 结构化输出（预告）

`model.with_structured_output(...)` 让模型直接返回 Pydantic 对象而非自由文本：

```python
from pydantic import BaseModel, Field

class MovieReview(BaseModel):
    title: str = Field(description="电影名")
    rating: float = Field(description="评分，1-10 分")
    summary: str = Field(description="一句话剧情简介")
    tags: list[str] = Field(description="标签列表，2-5 个")

# ⚠️ DeepSeek 必须加 method="function_calling"，不能用默认的 json_schema
review = model.with_structured_output(
    MovieReview,
    method="function_calling",
).invoke("评价电影《星际穿越》")
print(review.title, review.rating, review.tags)
```

> [!warning] DeepSeek 必须用 `method="function_calling"`
> DeepSeek 支持函数调用，但**不支持 json_schema 模式**。不加 `method` 参数时，`with_structured_output` 默认优先尝试 json_schema，会报错。详见 [[04-输出解析与结构化输出]] 和 [[12-常见问题与避坑指南]]。

> 对应代码：`code/02_models.py` 的 `part3_structured()`。

---

## 6. 嵌入模型（Embeddings）——RAG 的前置知识

聊天模型生成文本；**嵌入模型把文本变成向量**（一串数字），用于相似度检索。

### 6.1 标准用法（以 OpenAI 为例）

```python
from langchain.embeddings import init_embeddings

embeddings = init_embeddings("openai:text-embedding-3-small")
vec = embeddings.embed_query("LangChain 是什么？")
print(len(vec))          # 1536 维向量
print(vec[:5])           # [-0.021, 0.013, ...]
```

- **embed_query()**：编码"查询文本"（RAG 检索时用）
- **embed_documents()**：编码"知识文档"（入库时用，一次多条）

### 6.2 DeepSeek 的限制

> [!warning] DeepSeek 不提供 embedding 接口
> 本项目默认用 DeepSeek 做**对话模型**，但 DeepSeek **没有 embedding 服务**。
> 所以 RAG 篇（[[08-RAG检索增强生成]]）改用**本地 HuggingFace embedding**（`BAAI/bge-small-zh-v1.5`，免 Key、离线、首次自动下载约 95MB）。
>
> `code/02_models.py` 的 `part4_embeddings()` 用 try/except 包住了 OpenAI embedding 调用，DeepSeek-only 环境下会自动跳过，不影响其它部分。

向量相似 → 语义相近。这正是 [[08-RAG检索增强生成]] 的基石。

---

## ✅ 动手练习

1. 用 `.env` 配置好 DeepSeek，跑通 `code/02_models.py` 的 4 个 part；
2. 分别用 `invoke` 和 `stream` 调用，感受输出差异；
3. 打印 `resp.content_blocks`，观察内容块结构；
4. 用 `with_structured_output(..., method="function_calling")` 让模型返回一个你自定义的 Pydantic 结构；
5. （选做）如果你有 OpenAI Key，试试 `part4_embeddings` 的嵌入向量。

---

🏷️ `#LangChain` `#模型调用` `#ChatModel` `#DeepSeek`

[[README|← 返回学习路径总览]] ｜ [[03-Prompt工程与模板|下一篇：Prompt 工程 →]]
