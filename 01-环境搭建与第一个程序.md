# 01 · 环境搭建与第一个程序

> 从零装好环境，跑通第一个 LangChain 1.0 程序。
> 上一篇：[[00-学习路径与生态全景]] ｜ 下一篇：[[02-模型调用基础]]

---

## 1. 环境要求

| 依赖 | 要求 | 说明 |
|------|------|------|
| Python | **3.10+**（推荐 3.11/3.12） | LangChain 1.0 已放弃 Python 3.9 |
| pip | 最新版 | `python -m pip install --upgrade pip` |
| 模型 API Key | 任选一家 | 本项目默认 DeepSeek；Ollama 可免 Key |

> [!warning] 千万别用系统 Python 直接装！
> 用**虚拟环境（venv）**隔离项目依赖，避免包版本互相污染。这是所有 Python 项目的第一纪律。

---

## 2. 创建虚拟环境并安装

### 2.1 创建虚拟环境

```bash
# Windows (PowerShell)
python -m venv .venv
.venv\Scripts\activate

# macOS / Linux
python3 -m venv .venv
source .venv/bin/activate
```

激活成功后，命令行前缀会出现 `(.venv)`。

### 2.2 安装依赖

本项目已提供 `code/requirements.txt`，一键安装即可：

```bash
pip install -r code/requirements.txt
```

如需手动装，按场景选择：

| 场景 | 安装命令 |
|------|----------|
| 只调模型 + 基本链 | `pip install langchain langchain-openai` |
| 要用 RAG / 文档加载器 | 追加 `langchain-community` `langchain-chroma` `langchain-text-splitters` |
| RAG 的本地 embedding | 追加 `langchain-huggingface` `sentence-transformers` |
| 要做 Agent / 工作流 | 追加 `langgraph` |
| 追踪调试 | 追加 `langsmith` |
| 读 .env 配置 | 追加 `python-dotenv` |

### 2.3 验证安装

```bash
python -c "import langchain; print(langchain.__version__)"
# 输出类似：1.0.x
```

---

## 3. 配置模型（`.env` 四件套）

本项目所有示例统一从项目根的 `.env` 读取配置，**默认走 DeepSeek**（国内模型，便宜好用，兼容 OpenAI 协议，无需科学上网）。

### 3.1 创建 `.env`

复制模板：

```bash
cp .env.example .env        # Windows: copy .env.example .env
```

填入四件套：

```ini
API_KEY=sk-你的deepseek密钥
MODEL_NAME=deepseek-chat
MODEL_PROVIDER=openai            # DeepSeek 兼容 OpenAI 协议，provider 仍填 openai
BASE_URL=https://api.deepseek.com/v1
```

> ⚠️ `.env` 已加入 `.gitignore`，**永远不要把 API Key 提交到代码仓库**。

### 3.2 代码里怎么读

每个示例顶部固定写法：

```python
from dotenv import load_dotenv
load_dotenv()                          # 自动读取 .env
import os
api_key = os.getenv("API_KEY", "")
model_name = os.getenv("MODEL_NAME", "deepseek-chat")
model_provider = os.getenv("MODEL_PROVIDER", "openai")
base_url = os.getenv("BASE_URL", "https://api.deepseek.com/v1")
```

### 3.3 想换模型？只改 `.env`，代码不动

| 模型 | MODEL_PROVIDER | MODEL_NAME | BASE_URL | API_KEY |
|------|----------------|------------|----------|--------|
| DeepSeek（默认） | `openai` | `deepseek-chat` | `https://api.deepseek.com/v1` | 你的 key |
| 通义千问 | `openai` | `qwen-plus` | `https://dashscope.aliyuncs.com/compatible-mode/v1` | 你的 key |
| 智谱 GLM | `openai` | `glm-4-flash` | `https://open.bigmodel.cn/api/paas/v4` | 你的 key |
| OpenAI | `openai` | `gpt-4o-mini` | （留空用默认） | 你的 key |
| Ollama（本地，免 Key） | `ollama` | `qwen2.5:7b` | `http://localhost:11434` | 留空 |

> [!tip] Ollama 是唯一完全免 Key 的选项
> 它把模型跑在你自己电脑上，离线免费。需先安装 Ollama（https://ollama.com）并 `ollama pull qwen2.5:7b`。

### 3.4 DeepSeek 的已知限制

详见 [[12-常见问题与避坑指南]]：
1. **预付费**：账号余额不足会返回 `402 Insufficient Balance`，key 有效 ≠ 能调用。
2. **无 embedding 接口**：RAG 必须改用本地 HuggingFace embedding（见 [[08-RAG检索增强生成]]）。
3. **`trim_messages` 有限制**：见 [[06-记忆与多轮对话]]。

---

## 4. 第一个程序：Hello LangChain

```python
# code/01_hello_langchain.py（精简版，默认走 DeepSeek）
import os
from dotenv import load_dotenv
from langchain.chat_models import init_chat_model

load_dotenv()

api_key = os.getenv("API_KEY", "")
model_name = os.getenv("MODEL_NAME", "deepseek-chat")
model_provider = os.getenv("MODEL_PROVIDER", "openai")
base_url = os.getenv("BASE_URL", "https://api.deepseek.com/v1")

# Ollama 是本地模型，不需要 Key；其它远程模型必须有 Key
if model_provider != "ollama" and not api_key:
    raise SystemExit("❌ 未检测到 API Key，请先配置 .env（见笔记第 3 节）")

model = init_chat_model(
    model_name,
    model_provider=model_provider,
    api_key=api_key,
    base_url=base_url,
)

resp = model.invoke("用一句话介绍 LangChain")
print(resp.content)
```

> 完整版见 `code/01_hello_langchain.py`，额外包含：Ollama 分支的专门处理、缺 Key 时的友好报错（而不是一长串 traceback）、响应对象结构打印。

**运行：**

```bash
python code/01_hello_langchain.py
```

**预期输出：**

```
=== 模型回复 ===
LangChain 是一个用于构建由大语言模型驱动应用的开发框架，它通过统一的抽象接口连接模型、
提示词、工具和数据源，帮助你快速搭建智能应用。

=== 响应类型 ===
<class 'langchain_core.messages.ai.AIMessage'>
usage: {'input_tokens': 10, 'output_tokens': 35, 'total_tokens': 45}
```

### 逐行拆解

| 代码 | 作用 |
|------|------|
| `load_dotenv()` | 读取 `.env`，把配置注入环境变量 |
| `init_chat_model(...)` | 1.0 的统一模型入口，跨厂商一行切换 |
| `model.invoke(...)` | 发送消息并返回完整结果（`AIMessage`） |
| `resp.content` | 取模型回复的文本内容 |
| `resp.usage_metadata` | token 用量统计 |

> [!note] 底层发生了什么？
> `init_chat_model` 拿到模型名和 Key 后，内部实例化 `ChatOpenAI`（DeepSeek 兼容 OpenAI 协议，所以 provider 填 `openai` + `base_url` 指向 DeepSeek）并封装为统一的 `BaseChatModel`。你看不到任何 HTTP 细节——这就是抽象层的意义。

> [!warning] 报 `402 Insufficient Balance`？
> 这不是代码问题，是 **DeepSeek 账号余额不足**。DeepSeek 是预付费模式，去 https://platform.deepseek.com 充值即可（充 ¥10 就够练手）。

---

## 5. 常见安装问题

| 问题 | 解决 |
|------|------|
| `ModuleNotFoundError: No module named 'dotenv'` | `pip install python-dotenv` |
| `openai.OpenAIError: Missing credentials` | 没配 `.env` 或 `API_KEY` 为空，见第 3 节 |
| `402 Insufficient Balance` | DeepSeek 余额不足，去平台充值 |
| RAG 篇报 embedding 相关错 | DeepSeek 无 embedding，RAG 用本地 HuggingFace（见 [[08-RAG检索增强生成]]） |

更多问题见 [[12-常见问题与避坑指南]]。

---

## ✅ 动手练习

1. 建好虚拟环境，`pip install -r code/requirements.txt`，打印出 langchain 版本号；
2. 配置 `.env`（DeepSeek 或其它模型），跑通 Hello World；
3. 把 Hello World 里的问题换成你自己的问题，观察 `resp` 完整对象有哪些字段（试试 `print(resp)`）；
4. （进阶）把 `.env` 里的 `MODEL_PROVIDER` 改成另一家（或 Ollama），体验"改配置不改代码"就切换模型。

---

🏷️ `#LangChain` `#环境搭建` `#入门` `#DeepSeek`

[[README|← 返回学习路径总览]] ｜ [[02-模型调用基础|下一篇：模型调用 →]]
