# 01 · 环境搭建与第一个程序

> 从零装好环境，跑通第一个 LangChain 1.0 程序。
> 上一篇：[[00-学习路径与生态全景]] ｜ 下一篇：[[02-模型调用基础]]

---

## 1. 环境要求

| 依赖 | 要求 | 说明 |
|------|------|------|
| Python | **3.10+**（推荐 3.11/3.12） | LangChain 1.0 已放弃 Python 3.9 |
| pip | 最新版 | `python -m pip install --upgrade pip` |
| 模型 API Key | 任选一家 | OpenAI / DeepSeek / 通义 / 智谱… |

> [!warning] 千万别用系统 Python 直接装！
> 用**虚拟环境（venv）**隔离项目依赖，避免包版本互相污染。这是所有 Python 项目的第一纪律。

---

## 2. 创建虚拟环境并安装

### 2.1 创建虚拟环境

```bash
# Windows (PowerShell)
python -m venv .venv
.venv\Scripts\activate

# macOS / Linux
python3 -m venv .venv
source .venv/bin/activate
```

激活成功后，命令行前缀会出现 `(.venv)`。

### 2.2 安装 LangChain 1.0

```bash
# 最小安装：主包 + OpenAI 集成
pip install langchain langchain-openai

# 如果要用国内模型（DeepSeek 兼容 OpenAI 协议，直接装 openai 包即可）
# 或安装完整常用套件：
pip install langchain langchain-openai langchain-community langgraph
```

| 场景 | 安装命令 |
|------|----------|
| 只调模型 + 基本链 | `pip install langchain langchain-openai` |
| 要用 RAG / 文档加载器 | 追加 `pip install langchain-community` |
| 要做 Agent / 工作流 | 追加 `pip install langgraph` |
| 追踪调试 | 追加 `pip install langsmith` |

### 2.3 验证安装

```bash
python -c "import langchain; print(langchain.__version__)"
# 输出类似：1.0.x
```

> 本项目已提供 `code/requirements.txt`，可直接 `pip install -r code/requirements.txt`。

---

## 3. 配置 API Key（三种方式）

### 方式一：环境变量（推荐，最安全）

```bash
# Windows PowerShell
$env:OPENAI_API_KEY = "sk-你的key"

# macOS / Linux
export OPENAI_API_KEY="sk-你的key"
```

### 方式二：写入 .env 文件（配合 python-dotenv）

在项目根目录创建 `.env`：

```ini
OPENAI_API_KEY=sk-你的key
```

```python
from dotenv import load_dotenv
load_dotenv()   # 自动读取 .env
```

> ⚠️ `.env` 必须加入 `.gitignore`，**永远不要把 API Key 提交到代码仓库**。

### 方式三：代码中直接传（仅限本地练习）

```python
model = init_chat_model("gpt-4o-mini", api_key="sk-你的key")
```

---

## 4. 第一个程序：Hello LangChain

```python
# code/01_hello_langchain.py
from langchain.chat_models import init_chat_model

# 1. 初始化模型（1.0 统一入口，自动读 OPENAI_API_KEY）
model = init_chat_model("gpt-4o-mini", model_provider="openai")

# 2. 调用模型
resp = model.invoke("用一句话介绍 LangChain")
print(resp.content)
```

**运行：**

```bash
python code/01_hello_langchain.py
```

**预期输出：**

```
LangChain 是一个用于构建由大语言模型驱动应用的开发框架，它通过统一的抽象接口连接模型、
提示词、工具和数据源，帮助你快速搭建智能应用。
```

### 逐行拆解

| 代码 | 作用 |
|------|------|
| `init_chat_model(...)` | 1.0 的统一模型入口，跨厂商一行切换 |
| `model.invoke(...)` | 发送消息并返回完整结果（`AIMessage`） |
| `resp.content` | 取模型回复的文本内容 |

> [!note] 底层发生了什么？
> `init_chat_model` 拿到模型名和 Key 后，内部实例化 `ChatOpenAI` 并封装为统一的 `BaseChatModel`。
> 你看不到任何 HTTP 细节——这就是抽象层的意义。

---

## 5. 国内模型配置（DeepSeek / 通义 / 智谱）

国内用户不需要科学上网也能用 LangChain。以 **DeepSeek** 为例（OpenAI 兼容协议）：

```python
# 方式一：环境变量
# $env:DEEPSEEK_API_KEY = "sk-你的deepseek-key"

model = init_chat_model(
    "deepseek-chat",
    model_provider="openai",              # 协议兼容 OpenAI
    base_url="https://api.deepseek.com/v1",  # 指向 DeepSeek 的地址
)

resp = model.invoke("你好，介绍一下你自己")
print(resp.content)
```

> 或者更简洁：直接设 `OPENAI_API_KEY=你的deepseek key` + `OPENAI_BASE_URL=https://api.deepseek.com/v1`，代码完全不用改。

其他国内模型类似：
- 通义千问（DashScope）：`base_url="https://dashscope.aliyuncs.com/compatible-mode/v1"`
- 智谱 GLM：`base_url="https://open.bigmodel.cn/api/paas/v4"`
- 本地 Ollama：`model_provider="ollama"`（免费、离线，见 [[02-模型调用基础]]）

---

## 6. 目录规划建议

```bash
my-langchain-project/
├── .venv/            # 虚拟环境（不提交）
├── .env              # API Key（不提交）
├── requirements.txt  # 依赖清单
├── code/             # 示例代码（本教程结构）
└── notes/            # 你自己的笔记
```

---

## ✅ 动手练习

1. 建好虚拟环境，安装 langchain 1.0，打印出版本号；
2. 配置至少一家模型的 API Key，跑通 Hello World；
3. 把 Hello World 里的问题换成你自己的问题，观察 `resp` 完整对象有哪些字段（试试 `print(resp)`）；
4. （进阶）尝试把 `model_provider` 换成另一家，体验"一行切换模型"。

---

🏷️ `#LangChain` `#环境搭建` `#入门`

[[README|← 返回学习路径总览]] ｜ [[02-模型调用基础|下一篇：模型调用 →]]
