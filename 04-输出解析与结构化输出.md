# 04 · 输出解析与结构化输出

> 把模型的自由文本变成可编程的 JSON / Pydantic 对象——生产应用的必备能力。
> 对应代码：`code/04_structured_output.py`（4 个 part）。
> 上一篇：[[03-Prompt工程与模板]] ｜ 下一篇：[[05-LCEL表达式语言]]

---

## 1. 为什么需要结构化输出

模型默认返回自由文本，而程序需要的是**确定结构的数据**：

```
❌ 模型返回：嗯...我认为苹果公司2024年营收是3910亿美元左右，你们觉得呢？
✅ 期望返回：{"company": "Apple", "year": 2024, "revenue_usd": 391000000000, "confidence": 0.85}
```

没有结构化输出，你的程序就要靠字符串正则去抠数据——**脆弱、易错、不可维护**。

| 场景 | 需要结构化输出的原因 |
|------|----------------------|
| 调用下游 API | 需要确定字段 |
| 存入数据库 | 需要类型匹配 |
| 前端渲染 | 需要固定结构 |
| Agent 工具调用 | 参数必须标准（[[07-工具与函数调用]]） |

---

## 2. 最简方式：`with_structured_output`（1.0 推荐）

### 2.1 定义 Pydantic 模型 + 一句话调用

```python
from pydantic import BaseModel, Field
from langchain.chat_models import init_chat_model

model = init_chat_model(
    os.getenv("MODEL_NAME", "deepseek-chat"),
    model_provider=os.getenv("MODEL_PROVIDER", "openai"),
    api_key=os.getenv("API_KEY", ""),
    base_url=os.getenv("BASE_URL", "https://api.deepseek.com/v1"),
)

# ① 用 Pydantic 定义"期望的输出结构"
class MovieReview(BaseModel):
    title: str = Field(description="电影名")
    rating: float = Field(description="评分，1-10 分")
    summary: str = Field(description="一句话剧情简介")
    tags: list[str] = Field(description="标签列表，2-5 个")

# ② 一句话获得结构化输出
review = model.with_structured_output(
    MovieReview,
    method="function_calling",   # ⚠️ DeepSeek 必须加！见下方说明
).invoke("评价电影《星际穿越》")
print(review.title)      # 星际穿越
print(review.rating)     # 9.3
print(review.tags)       # ['科幻', '硬核', '亲情']
print(review.model_dump())  # 转 dict
```

**返回的是 Pydantic 对象**——字段可访问、可校验、可序列化（`.model_dump()`）。

> 对应代码：`code/04_structured_output.py` 的 `part1_with_structured()`。

> [!warning] DeepSeek 必须加 `method="function_calling"`
> `with_structured_output` 默认优先尝试 `json_schema` 模式，但 **DeepSeek 不支持 json_schema**，会报错。
> DeepSeek 支持 **function calling**，所以必须显式指定 `method="function_calling"`。
> OpenAI 官方模型三种都支持，可不加 method。详见 [[12-常见问题与避坑指南]]。

### 2.2 底层原理：Function Calling

`with_structured_output(method="function_calling")` 背后利用模型的**函数调用（tool calling）能力**：

1. 框架把你的 Pydantic 结构转换成 JSON Schema，伪装成"工具"传给模型；
2. 模型原生返回结构化 JSON 参数（而不是自由文本）；
3. 框架把 JSON 反序列化回 Pydantic 对象。

```
模型原生能力支持 → 100% 可靠
模型不支持函数调用 → 框架自动降级为"提示词 + 解析"
```

> [!note] 为什么比"让模型输出 JSON 再解析"可靠？
> 函数调用是模型**原生训练过的能力**，返回的 JSON 几乎不会格式错误。
> 而"请输出 JSON"的文本指令，经常遇到 JSON 里夹注释、缺引号、跑题文本等问题。

---

## 3. Output Parser（输出解析器）：手动控制的完整版

`with_structured_output` 是封装好的"快车道"。了解底层 `OutputParser` 才能应付定制需求：

### 3.1 常用解析器一览

| 解析器 | 作用 | 适用场景 |
|--------|------|----------|
| `StrOutputParser` | 只取文本（丢弃消息包装） | 最常见的"啥都不要只要字" |
| `JsonOutputParser` | 解析 JSON 字符串 | 通用 |
| `PydanticOutputParser` | 解析成 Pydantic 对象 | 带校验的结构化输出 |
| `CommaSeparatedListOutputParser` | 逗号分隔的列表 | 简单列表 |
| `MarkdownListOutputParser` | Markdown 列表 | 文档类 |

### 3.2 手动组装流程（理解原理）

```python
from langchain_core.output_parsers import PydanticOutputParser
from langchain_core.prompts import ChatPromptTemplate

parser = PydanticOutputParser(pydantic_object=MovieReview)

prompt = ChatPromptTemplate.from_messages([
    ("system", "你输出严格的 JSON。{format_instructions}"),
    ("human", "{input}"),
]).partial(format_instructions=parser.get_format_instructions())
# ↑ parser 自动生成"必须输出什么格式"的指令并注入 Prompt

chain = prompt | model | parser    # LCEL 管道（见笔记 05）
result = chain.invoke({"input": "评价电影《肖申克的救赎》"})
print(type(result))                # MovieReview
```

> 对应代码：`code/04_structured_output.py` 的 `part2_parser_manual()`。

> [!tip] `partial()` 方法
> `partial` 可以**预填充模板变量**——上面把"格式指令"固定住，调用时只需传 `input`。

### 3.3 注意：Prompt 里放 JSON 示例的坑

```python
# ❌ 反模式：JSON 里的 {} 会被当成模板变量
PromptTemplate.from_template('返回格式：{"name": "{name}"}')   # 报错！

# ✅ 正确姿势：{{ }} 转义，或用 json.dumps + partial 注入
PromptTemplate.from_template('返回格式：{{"name": "..."}}')
```

> 模板引擎中 `{{` 和 `}}` 表示**字面花括号**。JSON 里凡是不想被当作变量的 `{}`，都要写成 `{{ }}`。
> 更推荐：把 JSON 示例用 `.partial()` 作为**变量**注入，避免转义地狱。

---

## 4. 结构化输出的高级用法

### 4.1 嵌套结构 + 枚举约束（复杂对象）

```python
from typing import Literal

class Paper(BaseModel):
    title: str
    year: int
    field: Literal["NLP", "CV", "RL", "其他"]   # 枚举约束，约束模型只能在这几个值里选

class PaperList(BaseModel):
    papers: list[Paper]       # 顶层包一层列表

result = model.with_structured_output(
    PaperList,
    method="function_calling",   # ⚠️ DeepSeek 同样要加
).invoke("列举 3 篇经典 Transformer 相关论文")
for p in result.papers:
    print(f"  {p.year} [{p.field}] {p.title}")
```

> 对应代码：`code/04_structured_output.py` 的 `part3_nested()`。

### 4.2 只需 JSON dict 不要校验：`JsonOutputParser`

```python
from langchain_core.output_parsers import JsonOutputParser

prompt = ChatPromptTemplate.from_messages([
    ("human", "输出 JSON 格式：{input}"),
])
result = (prompt | model | JsonOutputParser()).invoke(
    {"input": "评价电影《盗梦空间》，字段: title, rating, summary, tags"}
)
print(type(result))   # dict（无类型校验，灵活但不保证字段齐全）
```

> 对应代码：`code/04_structured_output.py` 的 `part4_json_parser()`。

### 4.3 `method` 参数详解

| method | 说明 | DeepSeek | OpenAI |
|--------|------|----------|--------|
| `function_calling` | 走函数调用，最可靠 | ✅ 支持 | ✅ 支持 |
| `json_mode` | 要求模型输出纯 JSON（某些模型专属） | ❌ | ⚠️ 部分 |
| `json_schema` | 传入 schema 约束（OpenAI 结构化输出专属） | ❌ 不支持 | ✅ 支持 |

> [!warning] 本项目默认 DeepSeek，所有 `with_structured_output` **都必须加 `method="function_calling"`**。
> 换成 OpenAI 官方模型时可以省略（默认走 json_schema），但加了也无害。

---

## 5. 与 LCEL 结合（预告）

```python
from langchain_core.output_parsers import StrOutputParser

# 最经典的"三件套"管道：模板 → 模型 → 纯文本
chain = prompt | model | StrOutputParser()

# 结构化版：模板 → 模型 → Pydantic 对象
chain = prompt | model | PydanticOutputParser(pydantic_object=MovieReview)
```

> 📖 LCEL 管道机制（`|` 运算符、Runnable 协议）见 [[05-LCEL表达式语言]]。

---

## ✅ 动手练习

1. 定义 `Book` 结构（书名、作者、出版年、标签列表），用 `with_structured_output(method="function_calling")` 让模型输出一本书信息；
2. 手动搭一遍"模板 + PydanticOutputParser"管道，打印 `format_instructions` 看看模型实际收到的指令；
3. 用 `JsonOutputParser` 对比 `PydanticOutputParser` 的区别（有无类型校验）；
4. 故意定义一个错误字段类型（如 rating 写成 str），观察 Pydantic 校验报错——体会"校验"的价值。

---

🏷️ `#LangChain` `#结构化输出` `#Pydantic` `#OutputParser` `#DeepSeek`

[[README|← 返回学习路径总览]] ｜ [[05-LCEL表达式语言|下一篇：LCEL →]]
