# 07 · 工具与函数调用

> 让模型"动手"：定义工具、理解 Tool Calling 循环、构建多工具协作。
> 上一篇：[[06-记忆与多轮对话]] ｜ 下一篇：[[08-RAG检索增强生成]]

---

## 1. 为什么需要工具

模型的局限：**知识停在训练时、无法访问实时/私有数据、无法执行动作**。

- ❌ 问"现在几点" → 模型只能编
- ❌ 问"我的数据库里有多少用户" → 模型看不见
- ❌ 说"帮我查个快递" → 模型无法调 API

**工具 = 模型的"手和脚"**。模型负责"思考决策"，工具负责"执行动作"：

```
用户问题 → 模型判断需要工具 → 生成工具调用（含参数） → 执行工具 → 结果回填 → 模型组织最终回答
```

> [!note] 关键认知
> 工具**执行的不是模型**，而是你的 Python 代码。模型只负责"决定调用哪个工具、传什么参数"。

---

## 2. 用 `@tool` 定义工具（1.0 推荐）

### 2.1 最简单的工具

```python
from langchain.tools import tool

@tool
def get_weather(city: str) -> str:
    """查询指定城市的天气情况。"""
    # 这里可以是任何 Python 代码：调 API、查数据库、读文件...
    return f"{city} 今天晴，25°C，适合出行。"
```

**@tool 做了什么？** 函数 + 文档字符串 + 类型注解 → 自动生成模型的工具描述（JSON Schema）：

```json
{
  "name": "get_weather",
  "description": "查询指定城市的天气情况。",
  "parameters": {
    "type": "object",
    "properties": {"city": {"type": "string"}},
    "required": ["city"]
  }
}
```

> [!warning] 三个必须
> 1. **函数必须有类型注解**（`city: str`）——参数 schema 靠它生成；
> 2. **必须有 docstring**——它就是模型理解工具用途的说明书；
> 3. **docstring 里的参数说明**——`:param city:` 形式的说明会进 schema，让模型传参更准。

### 2.2 复杂参数：用 Pydantic 定义

```python
from pydantic import BaseModel, Field

class OrderQuery(BaseModel):
    order_id: str = Field(description="订单号")
    include_items: bool = Field(default=True, description="是否包含商品明细")

@tool(args_schema=OrderQuery)
def query_order(order_id: str, include_items: bool = True) -> str:
    """查询订单状态与明细。"""
    return f"订单 {order_id} 已发货（明细: {include_items}）"
```

### 2.3 工具的运行时参数（1.0 新特性）

```python
from langchain_core.tools import InjectedToolArg

@tool
def send_email(recipient: str, sender: InjectedToolArg = None) -> str:
    """发送邮件。sender 由系统注入，不暴露给模型。"""
    ...
```

> `InjectedToolArg`：某些参数（如鉴权对象、DB 连接）**不该由模型填写**，标记后系统运行时注入。进阶知识点。

---

## 3. Tool Calling 循环：模型是怎么"调用工具"的

### 3.1 单轮工具调用

```python
from langchain.chat_models import init_chat_model

model = init_chat_model("gpt-4o-mini")
model = model.bind_tools([get_weather])    # ← 把工具"绑定"给模型

resp = model.invoke("北京天气怎么样？")
print(resp.tool_calls)
# [{'name': 'get_weather', 'args': {'city': '北京'}, 'id': 'call_xxx'}]
```

模型没有直接回答，而是返回了一个**工具调用意图**：调用 `get_weather`，参数 `city=北京`。

### 3.2 完整循环（手动实现，理解原理）

```python
def run_with_tool(user_input: str):
    messages = [HumanMessage(user_input)]
    resp = model.invoke(messages)

    while resp.tool_calls:                       # 只要模型想调工具
        messages.append(resp)                    # ① 把工具调用意图放入历史
        for call in resp.tool_calls:
            result = TOOL_MAP[call["name"]].invoke(call["args"])   # ② 真正执行工具
            messages.append(ToolMessage(result, tool_call_id=call["id"]))  # ③ 结果回填
        resp = model.invoke(messages)            # ④ 再问模型：现在能答了吗
    return resp.content

print(run_with_tool("北京天气怎么样？"))
# 模型：调用工具 → 拿到结果 → 组织回答 → 输出
```

> [!note] ToolCall 消息链路（关键！）
> 工具调用不是魔法，本质是一条消息链：
> `HumanMessage → AIMessage(带 tool_calls) → ToolMessage(带 tool_call_id) → AIMessage(最终回答)`
> **ToolMessage 的 `tool_call_id` 必须和 AIMessage 里的 `id` 对上**，否则模型会报错。手动实现时最容易错的就是这里——交给 Agent 框架就不会错。

---

## 4. 交给框架：`create_agent`（1.0 标准姿势）

手动循环容易错，LangChain 1.0 用 `create_agent` 封装了完整循环（基于 LangGraph）：

```python
from langchain.agents import create_agent
from langchain.chat_models import init_chat_model

model = init_chat_model("gpt-4o-mini")

# 多个工具一起给
@tool
def get_time() -> str:
    """获取当前时间。"""
    import datetime
    return str(datetime.datetime.now())

@tool
def search_web(query: str) -> str:
    """搜索网页获取最新信息。"""
    return f"关于{query}的最新报道：LangChain 1.0 已发布..."

agent = create_agent(
    model=model,
    tools=[get_weather, get_time, search_web],
    system_prompt="你是一个全能助手，需要外部信息时请使用工具。",
)

result = agent.invoke({"messages": [{"role": "user", "content": "现在是几点？北京天气如何？"}]})
print(result["messages"][-1].content)    # 最终回答
```

### 执行流程（模型视角）

```
用户: 现在是几点？北京天气如何？
思考: 需要 get_time 和 get_weather → 并行调用两个工具
观察: 工具返回结果
思考: 信息齐了 → 组织回答
输出: 现在是 20:30，北京今天晴，25°C...
```

> [!tip] `create_agent` 帮你自动处理了
> - 工具调用循环（含多工具并行、多轮迭代）
> - ToolCall 消息链路的 id 匹配
> - 结构化输出（`response_format=ToolStrategy(MyModel)` 指定最终输出结构）
> - 流式、异步、追踪
> - 底层是 LangGraph（[[10-LangGraph工作流编排]]），持久化/人工介入等高级能力可解锁

---

## 5. 工具设计最佳实践

| 原则 | 说明 | 示例 |
|------|------|------|
| **单一职责** | 一个工具做一件事，别塞多逻辑 | `query_order` 别顺带 `send_refund` |
| **docstring 写"何时用"** | 模型靠它决定选哪个工具 | `"仅当用户要求退款时调用"` |
| **参数给默认值** | 降低模型传参出错率 | `limit: int = 10` |
| **错误信息要友好** | 工具内部 try/except，返回可读消息 | `"订单不存在，请检查单号"` |
| **不要暴露敏感参数** | 内部凭证用 `InjectedToolArg` | 数据库连接、API 密钥 |
| **返回要精简** | 结果太大时先摘要 | 1000 行数据 → 返回统计 |

> [!warning] 工具数量的甜蜜点
> 工具过多会稀释模型的选择准确率。经验值：**常规场景 5–10 个工具**，超出建议分组/路由。

---

## 6. 多工具协作实战（示例：查天气 + 算日子）

```python
from datetime import date

@tool
def days_until(target: str) -> int:
    """计算距离目标日期还有多少天，target 格式 YYYY-MM-DD。"""
    today = date.today()
    target_date = date.fromisoformat(target)
    return (target_date - today).days

@tool
def weekday_of(target: str) -> str:
    """返回目标日期是星期几，target 格式 YYYY-MM-DD。"""
    return date.fromisoformat(target).strftime("%A")

agent = create_agent(model, tools=[days_until, weekday_of],
                     system_prompt="你是日期计算专家。")
result = agent.invoke({"messages": [{"role": "user", "content": "2026年国庆节是星期几？距离现在还有几天？"}]})
print(result["messages"][-1].content)
```

---

## ✅ 动手练习

1. 用 `@tool` 定义 3 个工具（如：计算器、当前时间、掷骰子），绑定模型手动走一遍 `bind_tools` 循环；
2. 故意构造一个需要**连续两次**工具调用的任务（第一次结果作为第二次输入），观察循环；
3. 用 `create_agent` 重写，对比手动实现，感受框架带来的简化；
4. 给工具设计一个"查不了就返回友好错误"的场景，验证模型能否处理工具报错；
5. （进阶）用 Pydantic 定义复杂参数工具，打印 `get_tool_description()` 看 schema。

---

🏷️ `#LangChain` `#工具调用` `#ToolCalling` `#Agent`

[[README|← 返回学习路径总览]] ｜ [[08-RAG检索增强生成|下一篇：RAG →]]
