# 06 · 记忆与多轮对话

> Chat Model 本身"没有记忆"，记忆是我们手动把历史消息塞回去的技术。
> 对应代码：`code/06_memory.py`（含 DeepSeek 下 trim_messages 避坑）。
> 上一篇：[[05-LCEL表达式语言]] ｜ 下一篇：[[07-工具与函数调用]]

---

## 1. 模型没有记忆：先理解本质

```python
model.invoke("我叫小明")          # 第一轮
model.invoke("我叫什么？")        # 第二轮 → 模型：？？？（它不记得）
```

**Chat Model 是"无状态"的**——每次调用都是独立请求。所谓"记忆"，就是：

> 把之前的对话历史，拼进下一次请求的 messages 里。

```python
# 记忆的本质（伪代码）
messages = [
    SystemMessage("你是助手"),
    HumanMessage("我叫小明"),     # ← 历史 1
    AIMessage("你好，小明！"),     # ← 历史 2
    HumanMessage("我叫什么？"),    # ← 当前问题
]
```

---

## 2. 手动管理历史（最透明的方式）

### 2.1 简单版：自己维护列表

```python
from langchain.messages import HumanMessage, AIMessage

history = []                      # 会话历史

def chat(user_input: str) -> str:
    history.append(HumanMessage(user_input))
    resp = model.invoke(history)  # 全量历史发给模型
    history.append(AIMessage(resp.content))
    return resp.content

print(chat("我叫小明"))
print(chat("我叫什么？"))          # 正确：小明
```

### 2.2 配合 MessagesPlaceholder（模板化）

```python
from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder

prompt = ChatPromptTemplate.from_messages([
    ("system", "你是助手"),
    MessagesPlaceholder("history"),     # ← 历史在这里插入
    ("human", "{input}"),
])
```

---

## 3. 用 `RunnableWithMessageHistory`（框架标准方案）

手动管理没问题，但 LangChain 提供了标准封装，**自动做历史存取**：

```python
from langchain_core.chat_history import InMemoryChatMessageHistory
from langchain_core.runnables.history import RunnableWithMessageHistory

store = {}   # session_id → 历史对象

def get_session_history(session_id: str) -> InMemoryChatMessageHistory:
    """按 session_id 取/建历史（可替换为数据库实现做持久化）"""
    if session_id not in store:
        store[session_id] = InMemoryChatMessageHistory()
    return store[session_id]

# 1. 搭链（注意：输入变量叫 input，历史占位叫 history）
chain = prompt | model | StrOutputParser()

# 2. 包装历史
chain_with_history = RunnableWithMessageHistory(
    chain,
    get_session_history,
    input_messages_key="input",
    history_messages_key="history",
)

# 3. 调用时指定 session_id（多用户/多会话隔离）
resp = chain_with_history.invoke(
    {"input": "我叫小明"},
    config={"configurable": {"session_id": "user-A"}},
)
```

### 关键概念

| 概念 | 说明 |
|------|------|
| `session_id` | 会话标识：不同 id 各自独立的记忆（多用户隔离） |
| `get_session_history` | 工厂函数：按 id 取/建历史对象 |
| `input_messages_key` | 当前用户输入对应的模板变量名 |
| `history_messages_key` | 历史插入的占位符名 |

> 对应代码：`code/06_memory.py` 的 `demo_memory()`，演示了 user-A / user-B 两个会话的记忆隔离。

> [!note] 思考题
> `RunnableWithMessageHistory` 帮你做了什么？——它每次自动：取出该 session 的历史 → 拼进 messages → 调用模型 → **把新往返追加进历史**。你只需要管"存哪"。

---

## 4. 历史裁剪：`trim_messages`（防爆上下文）

**问题**：对话越长，token 越多，费用越高，还可能超出模型上下文窗口。

**方案**：`trim_messages` 自动裁剪历史，保留最近的 N 条。

### 4.1 ⚠️ DeepSeek 的坑：不能用 `token_counter=model`

> [!warning] 关键避坑（本项目踩过）
> `trim_messages` 默认用 `token_counter` 参数统计 token 数。如果你传 `token_counter=model`（模型对象），它会调用 `model.get_num_tokens_from_messages()`。
> 但 **`ChatOpenAI` 只对官方 OpenAI 模型名实现了这个方法**，对 `deepseek-chat` 这类自定义模型名直接抛：
> ```
> NotImplementedError: get_num_tokens_from_messages() is not presently implemented for model deepseek-chat
> ```
> **解决办法**：传一个自定义的 token 计数器函数，而不是模型对象。

### 4.2 正确写法：自定义 `_count_tokens`

```python
from langchain.messages import trim_messages

def _count_tokens(messages) -> int:
    """简易 token 计数器（按字符数近似估算）。

    DeepSeek 等非官方 OpenAI 模型名无法用 model.get_num_tokens_from_messages()，
    这里用字符数近似替代，足以演示 trim_messages 的裁剪逻辑。
    生产环境若需精确计数，可换用 tiktoken。
    """
    total = 0
    for m in messages:
        content = m.content if hasattr(m, "content") else m.get("content", "")
        total += len(str(content))
    return total

trimmer = trim_messages(
    max_tokens=200,                    # 保留最近多少 token
    strategy="last",                   # 保留最新的
    token_counter=_count_tokens,       # ⚠️ 不能用 model！用自定义函数
    include_system=True,               # 保留系统消息
    allow_partial=False,               # 不截断到一半
)

# 裁剪历史
trimmed = trimmer.invoke(messages)
```

> 对应代码：`code/06_memory.py` 的 `demo_trim()` 和 `_count_tokens()`。

### 4.3 把 trimmer 接进链

```python
def build_chain(with_trimmer: bool = False):
    chain = prompt | model | StrOutputParser()
    if with_trimmer:
        trimmer = trim_messages(
            max_tokens=200,
            strategy="last",
            token_counter=_count_tokens,   # ⚠️ 自定义计数器
            include_system=True,
            allow_partial=False,
        )
        chain = trimmer | chain            # 裁剪器放在链前面
    return RunnableWithMessageHistory(chain, get_session_history,
                                      input_messages_key="input",
                                      history_messages_key="history")
```

> [!tip] 记忆策略进阶
> - **滑动窗口**：只保留最近 N 轮（上例）
> - **摘要压缩**：把久远对话用模型压缩成摘要再放回（`Summarization` 方案）
> - **向量检索**：像 RAG 一样检索"相关历史"（超长会话场景）

---

## 5. 持久化记忆：不只是内存

`InMemoryChatMessageHistory` 重启即失。生产环境需要**持久化存储**：

| 存储 | 场景 |
|------|------|
| `InMemoryChatMessageHistory` | 本地练习、单机 demo |
| Redis / SQL 自建 | 常规生产（自定义存储类） |
| LangGraph 的 checkpoint | 工作流级持久化（[[10-LangGraph工作流编排]]） |

```python
# 自定义存储示例（存到 Redis / 数据库 —— 只需实现 add_message 与 messages 属性）
class MyDatabaseHistory(BaseChatMessageHistory):
    def __init__(self, session_id: str, db): ...
    def add_message(self, message): ...    # 存
    @property
    def messages(self): ...                 # 取
```

> 把 `get_session_history` 换成返回数据库实现的版本，就完成了"多轮对话落库"。理解接口，万物皆可存。

---

## ✅ 动手练习

1. 不用框架，手写一个维护历史列表的对话循环；
2. 用 `RunnableWithMessageHistory` + 两个 session_id 验证记忆隔离（对应 `demo_memory()`）；
3. 给链加上 `trim_messages`（用 `_count_tokens` 而非 model），用长对话测试裁剪效果；
4. （进阶）实现一个存到 SQLite 的持久化历史类，重启进程后记忆还在。

---

🏷️ `#LangChain` `#记忆` `#多轮对话` `#ChatHistory` `#DeepSeek` `#避坑`

[[README|← 返回学习路径总览]] ｜ [[07-工具与函数调用|下一篇：工具 →]]
