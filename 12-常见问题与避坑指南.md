# 12 · 常见问题与避坑指南

> 学习与实战中高频踩坑点的汇总：报错排查、0.x→1.0 迁移对照、DeepSeek 专项避坑、性能优化。
> 上一篇：[[11-生产实践与LangSmith]] ｜ [[README|← 返回学习路径总览]]

---

## 1. 0.x → 1.0 迁移对照表（最重要的表）

网上教程大多基于 0.x，对照此表快速"翻译"：

| 旧写法（0.x） | 新写法（1.0） |
|---------------|---------------|
| `from langchain.chat_models import ChatOpenAI` | `from langchain.chat_models import init_chat_model` |
| `ChatOpenAI(model="gpt-4o-mini")` 直接构造 | `init_chat_model(name, model_provider=..., ...)` |
| `from langchain.llms import OpenAI` | 统一用 Chat Model |
| `from langchain.agents import AgentExecutor, create_react_agent` | `from langchain.agents import create_agent` |
| `from langchain.schema import HumanMessage` | `from langchain.messages import HumanMessage` |
| `from langchain.prompts import ...` | `from langchain_core.prompts import ...` |
| `from langchain.output_parsers import ...` | `from langchain_core.output_parsers import ...` |
| `from langchain.embeddings import OpenAIEmbeddings` | `from langchain.embeddings import init_embeddings`（DeepSeek 用 `HuggingFaceEmbeddings`，见下） |
| `from langchain.vectorstores import Chroma` | `from langchain_chroma import Chroma` |
| `from langchain.text_splitter import ...` | `from langchain_text_splitters import ...` |
| `from langchain.document_loaders import ...` | 部分在 `langchain` 内，多数在 `langchain_community.document_loaders` |
| `Chain` / `LLMChain` | LCEL（[[05-LCEL表达式语言]]） |

> [!tip] 判断代码新旧的小技巧
> 看到 `langchain.xxx`（没有 `_core`/`_community` 后缀）的老导入，先查 [[00-学习路径与生态全景]] 的包结构，再决定迁移到哪。

---

## 2. 高频报错速查表

### 2.1 导入错误

| 报错 | 原因 | 解决 |
|------|------|------|
| `ModuleNotFoundError: langchain.chat_models` | 0.x 代码在 1.0 跑 | 用 `init_chat_model` 重写 |
| `ModuleNotFoundError: langchain_community` | 没装社区包 | `pip install langchain-community` |
| `ModuleNotFoundError: langchain_openai` | 没装厂商包 | `pip install langchain-openai` |
| `ModuleNotFoundError: langchain_chroma` | 没装 Chroma 包 | `pip install langchain-chroma` |
| `ModuleNotFoundError: sentence_transformers` | 没装本地嵌入依赖 | `pip install sentence-transformers`（RAG 篇需要） |

### 2.2 运行时错误

| 报错 | 原因 | 解决 |
|------|------|------|
| `OpenAIError: AuthenticationError` | API Key 错/缺失 | 检查 `.env` 的 `API_KEY` 是否有效 |
| **`402 Payment Required`** | **DeepSeek 余额不足** | **充值 DeepSeek 账户**（见坑 1） |
| `RateLimitError` | 触发限流 | 降频、加缓存、换大配额 |
| `TimeoutError` | 请求超时 | 加大 `timeout`、检查网络 |
| `KeyError: 'xxx'`（模板渲染） | Prompt 变量没传 | 检查 `invoke` 的 dict 是否齐全 |
| `InvalidInputError` | LCEL 管道输入不匹配 | 检查前一步输出格式 |
| `OutputParserException` | 模型输出不符合解析格式 | 换 `with_structured_output`、加示例、提高 temperature |
| `pydantic.ValidationError` | 模型输出类型不符 | 检查字段类型定义、给模型更明确指令 |
| `RecursionError` / 死循环 | Agent 无限调工具 | 设置最大迭代轮数 |
| `NotImplementedError: get_num_tokens_from_messages` | DeepSeek + `trim_messages(token_counter=model)` | 自定义计数器函数（见坑 4） |

---

## 3. DeepSeek 专项避坑（本项目核心）

本项目所有示例默认走 DeepSeek（`.env` 配置），以下是实际踩过的坑：

### 坑 1：余额不足返回 402

DeepSeek 余额耗尽时，API 返回 HTTP **402 Payment Required**，报错信息可能是 `Error code: 402`。

**解决**：登录 https://platform.deepseek.com 查看余额并充值。报错排查时先排除余额问题，再查代码。详见 [[01-环境搭建与第一个程序]]。

### 坑 2：DeepSeek 没有 Embedding 接口

DeepSeek 兼容 OpenAI 的 Chat 协议，但**不提供 embedding 接口**。直接用 `init_embeddings("openai:...")` 或 `OpenAIEmbeddings` 会报错。

**解决**：RAG 篇改用**本地 HuggingFace 嵌入**：

```python
# ❌ DeepSeek 不支持
# embeddings = init_embeddings("openai:text-embedding-3-small")

# ✅ 用本地 HuggingFace 嵌入（无需 API，首次自动下载模型）
from langchain_huggingface import HuggingFaceEmbeddings
embeddings = HuggingFaceEmbeddings(model_name="BAAI/bge-small-zh-v1.5")
```

- 模型 `BAAI/bge-small-zh-v1.5` 专为中文优化，体积小（~100MB）
- 可在 `.env` 用 `EMBEDDING_MODEL` 切换其他模型
- 首次运行自动从 HuggingFace 下载，后续走本地缓存
- 详见 [[08-RAG检索增强生成]] 第 4.1 节

### 坑 3：`with_structured_output` 不支持 `json_schema`

DeepSeek 支持 function calling，但**不支持 `json_schema` 方式**的结构化输出。

```python
# ❌ DeepSeek 会报错
model.with_structured_output(TripPlan, method="json_schema")

# ✅ 必须用 function_calling 方式
model.with_structured_output(TripPlan, method="function_calling")

# ✅ 或者不传 method（默认会尝试，但显式指定更安全）
model.with_structured_output(TripPlan)
```

| method | DeepSeek | OpenAI（官方模型名） |
|--------|----------|---------------------|
| `function_calling` | ✅ 支持 | ✅ 支持 |
| `json_schema` | ❌ 不支持 | ✅ 支持（需官方模型名） |
| 不指定 | 可能尝试 json_schema 而报错 | 自动选 |

> Agent 的 `ToolStrategy` 底层用 function calling，DeepSeek 可用。详见 [[04-输出解析与结构化输出]] 和 [[09-Agent智能体]]。

### 坑 4：`trim_messages` 不能传 `token_counter=model`

DeepSeek（以及任何非官方 OpenAI 模型名）用 `trim_messages` 时，**不能传 `token_counter=model`**，否则抛 `NotImplementedError: get_num_tokens_from_messages()...`。

原因：`ChatOpenAI` 的 `get_num_tokens_from_messages()` 只对**官方模型名**（如 `gpt-4o`）实现了 token 计数，非官方名直接抛错。

**解决**：自定义一个 token 计数器函数：

```python
from langchain_core.messages import trim_messages

def _count_tokens(messages):
    """简易 token 计数器：按字符数估算（中文约 1 字 ≈ 1.5 token）"""
    total = 0
    for m in messages:
        content = getattr(m, "content", "") or ""
        total += int(len(content) * 1.5) + 20   # +20 是消息结构的固定开销
    return total

# ✅ 用自定义计数器，不用 model
trimmed = trim_messages(
    messages,
    max_tokens=500,
    token_counter=_count_tokens,    # ← 不是 token_counter=model
    strategy="last",
    start_on="human",
)
```

> 这个估算不精确（精确需装 tiktoken），但足够做"裁掉旧消息"的用途。
> 详见 [[06-记忆与多轮对话]] 第 3 节和 `code/06_memory.py` 的 `_count_tokens`。

---

## 4. 经典"坑"与破解（通用经验）

### 坑 5：模型输出 JSON 带注释/多余文本

```python
# ❌ 模型返回: "好的！以下是结果：\n```json\n{...}\n```"
# ✅ 解决: 用 with_structured_output 或 JsonOutputParser
from langchain_core.output_parsers import JsonOutputParser
chain = prompt | model | JsonOutputParser()
```

### 坑 6：Prompt 模板里 JSON 花括号冲突

```python
# ❌ PromptTemplate.from_template('返回：{"a": {value}}')  → 报错
# ✅ 用 {{ }} 转义，或 partial 注入
PromptTemplate.from_template('返回：{{"a": "{value}"}}')
```

### 坑 7：RAG 检索结果与问题无关

排查顺序：
1. 检查切分（chunk 太大/太小）→ 调参（[[08-RAG检索增强生成]]）
2. 检查 embedding 模型是否适配中文（本项目用 `bge-small-zh-v1.5`）
3. 检查相似度阈值 / TopK
4. 换 `mmr` 检索 / 加重排序

### 坑 8：Agent 工具调用了但报错"tool_call_id 不匹配"

手动实现工具循环时的经典错误——**ToolMessage 必须携带对应 `tool_call_id`**。

```python
ToolMessage(content=result, tool_call_id=call["id"])   # ← id 必须和 AIMessage 的匹配
```

> 直接用 `create_agent`（[[09-Agent智能体]]）就不会碰到这个坑。

### 坑 9：多轮对话"越聊越贵、越聊越傻"

历史无上限累积导致：
- 超上下文窗口报错
- 长尾干扰模型判断

**解决**：`trim_messages` 裁剪 + 摘要压缩（[[06-记忆与多轮对话]]）。注意 DeepSeek 要用自定义计数器（坑 4）。

### 坑 10：`temperature=0` 结果还是不稳定

模型推理/工具调用本身有随机性；且部分厂商忽略 `temperature=0`。
**解决**：评估时用多次采样取稳定结果；结构化输出用 function calling。

### 坑 11：中文乱码 / 编码问题

- Windows 控制台：`chcp 65001` 或 `PYTHONIOENCODING=utf-8`
- 读文件：`TextLoader(file_path, encoding="utf-8")`
- Python 脚本首行加 `# -*- coding: utf-8 -*-`

---

## 5. 性能优化清单（从大到小）

```
1. 缓存（最大收益）：重复问题命中缓存
2. 模型选择：简单任务用小模型（DeepSeek 已很便宜，但仍可按场景分级）
3. 并行化：LCEL 的 dict 并行、batch()、异步调用
4. 输入压缩：trim_messages、RAG TopK 控制
5. 流式输出：首 token 更快（用户体验提升）
6. 连接复用：HTTP 连接池、批处理请求
```

---

## 6. 模型配置速查（本项目专用）

本项目统一从 `.env` 读取配置，四个变量：

| 变量 | 作用 | DeepSeek 默认值 |
|------|------|----------------|
| `API_KEY` | 模型 API 密钥 | （你的 DeepSeek Key） |
| `MODEL_NAME` | 模型名 | `deepseek-chat` |
| `MODEL_PROVIDER` | 厂商标识 | `openai`（兼容协议） |
| `BASE_URL` | API 地址 | `https://api.deepseek.com/v1` |

切换其他厂商：

| 厂商 | MODEL_PROVIDER | BASE_URL | MODEL_NAME |
|------|---------------|----------|------------|
| DeepSeek | `openai` | `https://api.deepseek.com/v1` | `deepseek-chat` |
| 通义千问 | `openai` | `https://dashscope.aliyuncs.com/compatible-mode/v1` | `qwen-plus` |
| OpenAI | `openai` | （留空用默认） | `gpt-4o-mini` |
| Ollama（本地） | `openai` | `http://localhost:11434/v1` | `qwen2.5:7b` |

> RAG 篇额外支持 `EMBEDDING_MODEL` 环境变量切换本地嵌入模型（默认 `BAAI/bge-small-zh-v1.5`）。

---

## 7. 学习资源推荐（防"学错版本"）

| 资源 | 说明 |
|------|------|
| 官方文档（1.0） | https://docs.langchain.com —— 以官方为准 |
| 官方 Python 教程 | https://docs.langchain.com/docs/tutorials |
| 官方博客 1.0 发布说明 | https://blog.langchain.com/langchain-langgraph-1dot0 |
| 中文社区文档 | https://langchaincn.cn/docs.html |
| LangGraph 官网 | https://langchain-ai.github.io/langgraph |
| DeepSeek 文档 | https://api-docs.deepseek.com |

> ⚠️ 看第三方教程前，先确认它针对的版本。**2025 年 10 月前的中文教程大多是 0.x**，用本文的对照表"翻译"着看。

---

## 8. 自测题库（检验掌握度）

1. `init_chat_model` 相比直接实例化 `ChatOpenAI` 的优势是什么？
2. `create_agent` 底层用的什么框架？它的消息轨迹怎么查看？
3. LCEL 管道 `prompt | model | parser` 每一段的输入输出分别是什么？
4. RAG 五步流程是哪五步？chunk 大小对检索有什么影响？
5. `trim_messages` 解决什么问题？DeepSeek 用它时有什么坑？`MessagesPlaceholder` 解决什么问题？
6. DeepSeek 为什么不能直接用 `OpenAIEmbeddings`？本项目怎么解决的？
7. `with_structured_output` 在 DeepSeek 上必须指定哪个 `method`？
8. LangSmith 是什么？它和 DeepSeek 的 Key 是什么关系？无 LangSmith Key 时代码怎么降级？
9. 什么时候该用 LangGraph 而不是 `create_agent`？
10. 列出 3 个生产安全要点。

---

## ✅ 动手练习

1. 故意写错 API Key，复现认证报错，学会读错误信息；
2. 把一段 0.x 老代码（网上随便找）用迁移对照表改写成 1.0；
3. 给 RAG 链加缓存，比较两次调用的耗时；
4. 用 `time` 对比 `invoke` / `stream` / `batch` 的性能差异；
5. 逐条对照第 3 节的 DeepSeek 四大坑，在代码里找到对应的修复写法。

---

🏷️ `#LangChain` `#避坑指南` `#FAQ` `#DeepSeek` `#性能优化`

[[README|← 返回学习路径总览]]
